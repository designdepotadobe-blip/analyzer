// Used for `ng build` with the "production" configuration (Vercel's build runs
// this). REPLACE the URL below with your actual Railway backend URL once it's
// deployed — see the deployment guide, step "point the frontend at the backend".
export const environment = {
  production: true,
  apiBase: 'https://YOUR-RAILWAY-APP.up.railway.app',
};
