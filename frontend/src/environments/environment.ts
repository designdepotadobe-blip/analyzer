// Used by `ng build`/`ng serve` in dev (default configuration). Swapped for
// environment.prod.ts at build time via the `fileReplacements` entry in the
// "production" configuration in angular.json — see that file's comment.
export const environment = {
  production: false,
  apiBase: 'http://localhost:10000',
};
