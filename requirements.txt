# Kept identical to backend/requirements.txt (the file local dev/README use) —
# NOT a `-r backend/requirements.txt` reference: Railway's Nixpacks builder
# copies only requirements.txt into its dependency-install layer before the
# rest of the repo (including backend/) is copied in, so a cross-directory
# reference here fails with "No such file or directory" at build time. This
# has to be self-contained.
fastapi>=0.110
uvicorn[standard]>=0.27
yahooquery>=2.3
pandas>=2.0
numpy>=1.24
scipy>=1.10
pytickersymbols>=1.13
